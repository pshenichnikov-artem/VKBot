namespace VKBot.Features.Core.Domain.Entities
{
    public class Correspondence
    {
        //Message переписка между старостой и админом
        //SenderMessageId(от вк) // глобальный id вк
        // RecipientMessageId
        // SenderId
        // recipientId(проверка, что роль отправителя и получателя не совпадают)
        // text
    }
}
